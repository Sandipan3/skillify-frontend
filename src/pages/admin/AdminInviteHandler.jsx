import React from "react";
import { useParams } from "react-router-dom";

const AdminInviteHandler = () => {
  const params = useParams();
  return <div>AdminInviteHandler</div>;
};

export default AdminInviteHandler;
